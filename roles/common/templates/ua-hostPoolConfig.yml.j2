defaultHostCredentials:
  sshUserName: {{ ezusername }}
  sshPassword: {{ ezpassword_b64 }}
  sshPort: 22
hosts:
  - host: {{ master }}
    labels:
    - role: controlplane
  {% for worker in workers %}
- host: {{ worker }}
    labels:
    - role: worker
  {% endfor %}